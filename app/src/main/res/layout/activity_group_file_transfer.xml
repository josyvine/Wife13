<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/editorial_background">

    <!-- Top AppBar Toolbar -->
    <com.google.android.material.appbar.MaterialToolbar
        android:id="@+id/toolbarFileTransfer"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="@color/editorial_background"
        app:title="Group File Sharing"
        app:titleTextColor="@color/editorial_primary_dark"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Peer Selector Layout Container -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardPeerSelect"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/editorial_border"
        app:strokeWidth="1dp"
        app:cardBackgroundColor="@color/editorial_surface"
        app:layout_constraintTop_toBottomOf="@id/toolbarFileTransfer">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:id="@+id/tvPeerSelectLabel"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select Recipient Devices"
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="@color/editorial_primary_dark" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/editorial_border"
                android:layout_marginVertical="10dp" />

            <!-- Horizontal peer checkbox select recyclerview -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvPeerSelect"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:overScrollMode="never" />

            <!-- Fallback text displayed if no peers are connected -->
            <TextView
                android:id="@+id/tvNoPeersDetected"
                android:layout_width="match_parent"
                android:layout_height="50dp"
                android:gravity="center"
                android:text="No connected mesh devices detected."
                android:textColor="@color/editorial_text_secondary"
                android:textSize="12sp"
                android:visibility="gone" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Unified File Picker Controller Button -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/btnPickFile"
        android:layout_width="match_parent"
        android:layout_height="64dp"
        android:layout_marginHorizontal="16dp"
        android:layout_marginTop="12dp"
        app:cardCornerRadius="16dp"
        app:cardElevation="0dp"
        app:cardBackgroundColor="@color/editorial_surface"
        app:strokeColor="@color/editorial_border"
        app:strokeWidth="1dp"
        android:clickable="true"
        android:focusable="true"
        android:foreground="?android:attr/selectableItemBackground"
        app:layout_constraintTop_toBottomOf="@id/cardPeerSelect">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="horizontal"
            android:gravity="center"
            android:padding="8dp">

            <ImageView
                android:layout_width="24dp"
                android:layout_height="24dp"
                android:src="@drawable/mobile_share_stack_24px"
                app:tint="@color/editorial_accent"
                android:layout_marginEnd="12dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Select and Send File(s)"
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="@color/editorial_primary_dark" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Historical Transfers Container -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/cardHistory"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_margin="16dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="0dp"
        app:strokeColor="@color/editorial_border"
        app:strokeWidth="1dp"
        app:cardBackgroundColor="@color/editorial_surface"
        app:layout_constraintTop_toBottomOf="@id/btnPickFile"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:padding="16dp">

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Group Share History Log"
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="@color/editorial_primary_dark" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/editorial_border"
                android:layout_marginVertical="10dp" />

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/rvFileHistory"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:overScrollMode="never" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

    <!-- Overlay Progress Card Layer (Hidden by default, displayed only during active streams) -->
    <com.google.android.material.card.MaterialCardView
        android:id="@+id/layoutTransferProgress"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        app:cardCornerRadius="24dp"
        app:cardElevation="12dp"
        app:strokeColor="@color/editorial_border"
        app:strokeWidth="1dp"
        app:cardBackgroundColor="@color/editorial_surface"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="20dp">

            <!-- Summary header labels -->
            <TextView
                android:id="@+id/tvActiveFileName"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:text="Processing Transfer..."
                android:textStyle="bold"
                android:textSize="14sp"
                android:textColor="@color/editorial_primary_dark"
                android:ellipsize="end"
                android:maxLines="1" />

            <!-- Main Aggregate progress bar components -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:gravity="center_vertical"
                android:layout_marginTop="12dp">

                <ProgressBar
                    android:id="@+id/pbTransferPercentage"
                    style="@android:style/Widget.ProgressBar.Horizontal"
                    android:layout_width="0dp"
                    android:layout_height="6dp"
                    android:layout_weight="1"
                    android:max="100"
                    android:progress="0"
                    android:progressDrawable="@android:drawable/progress_horizontal" />

                <TextView
                    android:id="@+id/tvTransferPercentText"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginStart="12dp"
                    android:text="0%"
                    android:textSize="12sp"
                    android:textStyle="bold"
                    android:textColor="@color/editorial_accent" />
            </LinearLayout>

            <TextView
                android:id="@+id/tvTransferSpeedAndSize"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:text="0 B / 0 B (0.0 MB/s)"
                android:textSize="11sp"
                android:textColor="@color/editorial_text_secondary" />

            <View
                android:layout_width="match_parent"
                android:layout_height="1dp"
                android:background="@color/editorial_border"
                android:layout_marginVertical="12dp" />

            <TextView
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="Participant Progress Tracks"
                android:textSize="12sp"
                android:textStyle="bold"
                android:textColor="@color/editorial_primary_dark"
                android:layout_marginBottom="8dp" />

            <!-- Dynamic container where targeted device progress rows are inflated on-the-fly -->
            <LinearLayout
                android:id="@+id/containerActivePeersProgress"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical" />

        </LinearLayout>
    </com.google.android.material.card.MaterialCardView>

</androidx.constraintlayout.widget.ConstraintLayout>