<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:paddingVertical="8dp">

    <!-- Peer Identity and Percentage Header -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:layout_marginBottom="4dp">

        <TextView
            android:id="@+id/tvGroupPeerLabel"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Mesh Peer Device"
            android:textStyle="bold"
            android:textSize="12sp"
            android:textColor="@color/editorial_primary_dark"
            android:ellipsize="end"
            android:maxLines="1" />

        <TextView
            android:id="@+id/tvGroupPeerPercentText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="0%"
            android:textSize="11sp"
            android:textStyle="bold"
            android:textColor="@color/editorial_accent" />

    </LinearLayout>

    <!-- Progress bar allocated to this peer's socket channel -->
    <ProgressBar
        android:id="@+id/pbGroupPeerPercentage"
        style="@android:style/Widget.ProgressBar.Horizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:max="100"
        android:progress="0"
        android:progressDrawable="@android:drawable/progress_horizontal" />

    <!-- Detailed statistics showing byte counts and MB/s rates -->
    <TextView
        android:id="@+id/tvGroupPeerStats"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:text="0 B / 0 B (0.0 MB/s)"
        android:textSize="10sp"
        android:textColor="@color/editorial_text_secondary" />

</LinearLayout>